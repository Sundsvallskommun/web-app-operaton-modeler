# Base URL of the BFF (Express + Passport-SAML). The SPA never talks directly
# to api-service-operaton — every API call goes through the BFF, which holds
# the OAuth2 client_credentials and forwards as Bearer.
NEXT_PUBLIC_API_URL=http://localhost:3001/api
