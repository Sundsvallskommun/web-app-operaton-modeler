# Top-level docker-compose pointers. Per-service config (SAML certs, OAuth2
# client secret, session signing key, ...) lives in backend/.env.
COMPOSE_PROJECT_NAME=operaton-modeler
NODE_ENV=development
FRONTEND_PORT=3000
BACKEND_PORT=3001
